<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ("../partials/head") %>
    <link rel="stylesheet" href="/stylesheets/index.css">
</head>
<body>
    <div class="content">
        <%- include ("../partials/nav") %>
        <h1>Hello <%= username %></h1>
        <% if (assignments.length) { %>
            <h3>Assignments</h3>
            <% for (let a of assignments) { %>
                <a href= "/assignment/<%= a.ID %>">
                    <div class="assignment">
                        <% if (a.MARK != null) { %>
                            <% if (eval(a.MARK) == 1) { %>
                                <h4><%= a.NAME %> ✔️</h4>
                                <em style="color: limegreen;">You have submitted this assignment with full marks! (A score of <%= a.MARK %>)</em>
                            <% } else if (eval(a.MARK) > 0) { %>
                                <h4><%= a.NAME %> ✔️</h4>
                                <em style="color: gold;">You have submitted this assignment with a score of <%= a.MARK %>.</em>
                            <% } else { %>
                                <h4><%= a.NAME %> ❌</h4>
                                <em style="color: red;">You have submitted this assignment with a score of <%= a.MARK %>.</em>
                            <% } %>
                        <% } else { %>
                            <h4><%= a.NAME %></h4>
                            <em style="color: red;">You haven't attempted this assignment yet.</em>
                        <% } %>
                    </div>
                </a>
            <% } %>
        <% } else { %>
            <p>You don't currently have any pending assignments 🧐</p>
        <% } %>
    </div>
    <%- include ("../partials/footer") %>
</body>
</html>

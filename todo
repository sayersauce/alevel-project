Outputs are either lines or generated by teacher inputting example code.
Change workflow of making and setting assignments for admins as it is currently tedious.
Assignment groupings by headings, collapsible on front-page. Useful for assigning assignments for a whole term for example.
Feature to view student code in progress live with an IDE. And to be able to execute the code like a student with the tests.
AJAX code saving feature for students writing code, so progress isn't lost when refreshed.
Filters and improvements on the real-time code page.
Assignments changed to be made with exemplar code from the teacher, which is executed and passed inputs, automatically generating outputs.
Assignments changed to include starting code, so students do not get stuck on how to receive inputs for example.